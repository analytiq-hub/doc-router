# Credential kinds porting status

Bulk-generated JSON under this directory comes from `make credential-dump` → `make credential-port` (see `docs/docrouter_credentials.md` §10).

Track kinds that are intentionally **not** declarative JSON here:

| n8n / upstream key | Status | Notes |
|-------------------|--------|--------|
| *(none yet)* | — | Add rows when a kind is handler-only or out of scope |
