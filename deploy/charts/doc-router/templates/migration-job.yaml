apiVersion: batch/v1
kind: Job
metadata:
  name: doc-router-migrate
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "doc-router.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-upgrade,pre-rollback
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded,before-hook-creation
spec:
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: migrate
        image: {{ .Values.image.backend.repository }}:{{ .Values.image.backend.tag }}
        imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
        command: ["sh", "-c", "cd /app/packages/python && python3 migrate.py"]
        # Only the Secret is available at pre-install time (ConfigMap is created by Helm later).
        # migrate.py only needs MONGODB_URI and ENV from the Secret.
        envFrom:
        - secretRef:
            name: doc-router-secrets
