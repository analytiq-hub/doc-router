apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: doc-router
  labels:
    app: mongodb
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: mongodb
  clusterIP: None  # Headless service for StatefulSet
